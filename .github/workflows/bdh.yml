name: bdh CI

on:
  push:
    branches: [main]
    paths:
      - 'bdh/**'
      - '.github/workflows/bdh.yml'
  pull_request:
    branches: [main]
    paths:
      - 'bdh/**'
      - '.github/workflows/bdh.yml'

defaults:
  run:
    working-directory: bdh

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: bdh/go.mod
          cache-dependency-path: bdh/go.sum

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          binary=bdh
          if [ "$GOOS" = "windows" ]; then
            binary=bdh.exe
          fi
          go build -ldflags="-s -w" -o "$binary" ./cmd/bdh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bdh-${{ matrix.goos }}-${{ matrix.goarch }}
          path: bdh/bdh*
          if-no-files-found: error
